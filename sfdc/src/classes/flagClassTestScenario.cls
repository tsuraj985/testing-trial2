@isTest 
private class flagClassTestScenario { 
private static TestMethod void test1(){ 
flagclass oc = new flagclass(); 
Boolean bool=new flagclass.innerClass().exampleMethod(); 
system.assert(bool); 
} 
}